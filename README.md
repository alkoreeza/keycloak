# Keycloak + MySQL Project

## Overview
This project sets up Keycloak with MySQL using Docker Compose. Keycloak runs on port 8080 and MySQL on port 3306.

## Quick Start

1. Run the stack:
```bash
docker-compose up -d
